syntax = "proto3";

package notification;

option go_package = "generated/proto/notification";

import "google/protobuf/timestamp.proto";

enum NotificationType {
  EMAIL_VERIFIED = 0;
}

message NotificationEmailVerifiedPayload {
  string otp_code = 1;
  google.protobuf.Timestamp expired_at = 2;
}

message Notification {
  NotificationType type = 1;
  oneof data {
    NotificationEmailVerifiedPayload email_verified = 2;
  }
}